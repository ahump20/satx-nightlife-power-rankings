{
  "name": "@satx-nightlife/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:migrate": "wrangler d1 execute satx-nightlife-db --local --file=./src/db/schema.sql",
    "db:migrate:prod": "wrangler d1 execute satx-nightlife-db --file=./src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.1.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "vitest": "^1.2.0",
    "wrangler": "^3.24.0",
    "typescript": "^5.3.3"
  }
}
